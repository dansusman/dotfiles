update_gitnum() {
    local new_value="$1"
    echo "export GITNUM=\"$new_value\"" > ~/.gitnum_env
    source ~/.gitnum_env
}

# Handle arguments
branch_name="$1"
git switch "$branch_name"
update_gitnum "$2"
